const connection = require("../config/database");
const { v4: uuid } = require("uuid");

const serviceMethods = {};

serviceMethods.getAllMovies = () => {
  return;
};

serviceMethods.getOneMovie = (id) => {
  return;
};

serviceMethods.createMovie = (body) => {
  return;
};

serviceMethods.updateMovie = (body, id) => {
  return;
};

serviceMethods.deleteMovie = (id) => {
  return;
};

serviceMethods.getTheatreForMovie = (id) => {
  return;
};

serviceMethods.getShowTimeForMovie = (id) => {
  return;
};

module.exports = serviceMethods;
