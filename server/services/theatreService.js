const connection = require("../config/database");
const { v4: uuid } = require("uuid");

const serviceMethods = {};

serviceMethods.getAllTheatres = () => {
  return;
};

serviceMethods.getOneTheatre = (id) => {
  return;
};

serviceMethods.createTheatre = (body) => {
  return;
};

serviceMethods.updateTheatre = (body, id) => {
  return;
};

serviceMethods.deleteTheatre = (id) => {
  return;
};

serviceMethods.getMovieForTheatre = (id) => {
  return;
};

serviceMethods.getShowTimeForTheatre = (id) => {
  return;
};

module.exports = serviceMethods;
